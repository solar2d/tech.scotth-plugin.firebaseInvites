Sources for the plugin `plugin.firebaseInvites`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.firebaseInvites" = {
            publisherId = "tech.scotth",
        },
    },
}
```
